[project]
name = "autoresearch-packing"
version = "0.1.0"
description = "Autonomous 2D shape packing research loop (no ML)"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "numba",
    "joblib",
]